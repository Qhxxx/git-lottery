extends layout

block content
  h1= title
  p Welcome to #[em Lottery System].

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The Lottery System has the following record counts:

    ul
      li #[strong Prizes:] !{data.prize_count}
      li #[strong Projects:] !{data.project_count}
      